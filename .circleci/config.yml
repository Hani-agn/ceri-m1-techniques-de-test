version: 2.1

orbs:
  codecov: codecov/codecov@4.0.1

jobs:
  build-and-test:
    docker:
      - image: cimg/openjdk:17.0
    steps:
      - checkout
      - run:
          name: Run Maven Test
          command: mvn test

workflows:
  version: 2
  upload-to-codecov:
    jobs:
      - checkout  # Ajout du job checkout
      - build-and-test
      - codecov/upload:
          file: target/site/jacoco/jacoco.xml
          flags: unit
          name: codecov-umbrella
          token: $CODECOV_TOKEN

  build-and-test-on-master:
    jobs:
      - build-and-test:
          filters:
            branches:
              only: master
